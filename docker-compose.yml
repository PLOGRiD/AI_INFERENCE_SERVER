services:
  api:
    image: ${DOCKERHUB_USERNAME:-plogrid}/plogrid-ai:latest
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
    environment:
      - YOLO_MODEL_DIR=/app/models
      - NIR_MODEL_DIR=/app/models
